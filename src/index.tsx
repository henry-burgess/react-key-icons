import React, { FC, ReactElement } from "react";

const SIZE = 72;

const Key: FC<KeyProps> = (props: KeyProps): ReactElement => {
  return (
    <h1>Test</h1>
    // <svg>
    // </svg>
  );
};

export default Key;